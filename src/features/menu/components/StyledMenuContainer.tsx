import { Navbar } from 'react-bootstrap';
import styled from 'styled-components';

const StyledMenuContainer = styled(Navbar)`
  max-width: 100%;
  margin: 0;
  padding: 0 8px;
`;

export default StyledMenuContainer;
